# Items Marked for Deletion

This file lists unused packages, files, and folders that have been identified and can be safely removed from the project.

## Unused Packages

The following npm packages are not being used in the codebase and can be uninstalled:

- `dompurify`
- `lodash-es`
- `node-fetch`

To remove them, run the following command:

```bash
npm uninstall dompurify lodash-es node-fetch
```

## Unused Files

The following files are not being used and can be deleted:

- `extension/src/popupMinimal.tsx`
- `extension/src/server.ts`
- `extension/src/handlers.ts`
- `server/trpc/base.js`
- `server/trpc/router.js`
- `server/trpc/trpcContext.js`
- `server/env.js`
